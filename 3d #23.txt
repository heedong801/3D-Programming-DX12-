5/29	HLSL
 
벡터 데이터 형
벡터를 1차원 배열처럼 사용가능하다.
 
스위즐링의 의미, 한계점
마스킹의 의미와 스위즐링과의 차이, 한계점
 
행렬 데이터 형
배열표현으로는 스위즐링이 가능한가?
 
행렬을 쉐이더로 넘기기 위해서는 전치행렬이 필요되는 이유?
 
벡터와 행렬 수식
쉐이더에서 벡터/행렬의 사칙연산이 어떻게 적용되나? 요소별연산
 
쉐이더에서 제공하는 내장함수
dot, mul(행렬곱)
 
자료형(구조체), 자료형(Data Types)
보간에 대한 기본값 = 선형
 
텍스처 Skip
 
흐름제어
discard의 의미
Attribute를 쓰면 좋은점
사용 예 흐름제어(3번째장)
 
a0 = b0 + c0; ---- a999 = b999 + c999;
이 코드가 포루프보다 훨씬 빠름
 
함수
쉐이더 함수는 인라인함수이고, 재귀호출을 지원하지 않음
파라미터는 기본적으로 Call by value
in, out 등의 의미?
 
기하, 스트림 Skip
 
내장함수
모든파라미터에 대해 반환경까지 오버로드 되어있음. abs요소별 연산
예외로 외적같이 파라미터가 제한되어 있는 놈도 있음
 
clip(x) 반환값 x, x가 0보다 작으면 버림
if f < 0 discard == clip(f) 같은 의미이다.
 
접선(tangent)
접선에 수직인 선분(법선 Normal)
바이노멀, 바이탄젠트?
 
mul.x 가 행벡터, y가 열벡터 곱의 의미
 
따라하기 14, 15
Shaders.hlsl
 
cbuffer --- : reg(b0)
{
	matrix	--- : packoffset(c0);
}
 
메쉬클래스